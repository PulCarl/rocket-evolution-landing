name: Sync latest videos from YouTube

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Fetch latest videos from YouTube RSS
        run: node scripts/sync-videos.mjs

      - name: Commit changes if any
        run: |
          if git diff --quiet -- src/data/videos.json; then
            echo "No changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/videos.json
          git commit -m "Sync latest videos from YouTube"
          git push
